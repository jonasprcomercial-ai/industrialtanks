export interface Tanque {
  id: string;
  nome: string;
  capacidadeMaxima: number;
  volumeAtual: number;
  posX: number; // posição horizontal no mapa
  posY: number; // posição vertical no mapa
}   